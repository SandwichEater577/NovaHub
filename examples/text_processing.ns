# text_processing.ns
# Simple tokenization and count example
set text = "The quick brown fox jumps over the lazy dog"
# Split isn't native; simulate word count with a simple loop
set words = 0
set i = 0
while [i < 100] {
  # naive search for spaces to count words; this example is illustrative
  if [i == 0] then [set words = 1]
  set i = i + 1
  if [i >= 40] then [exit]
}
print "This script is a placeholder for text processing examples"
